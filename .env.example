# IFROF - Environment Variables (Example)
# Copy to .env (do NOT commit .env)

# App
NODE_ENV=development
APP_URL=http://localhost:5173

# Database (Drizzle)
DATABASE_URL=postgresql://user:password@localhost:5432/ifrof

# Auth / Sessions
AUTH_SECRET=change_me_long_random_string

# Payments (Stripe) - feature gating only (no webhook verification required)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_BASIC=price_...
STRIPE_PRICE_PREMIUM=price_...

# AI Provider (optional)
AI_PROVIDER_KEY=...
